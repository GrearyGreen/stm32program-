如果单独使用蓝牙模块，需要准备
timer.c初始化定时器7，具体不了解
timer.h
usart3.c初始化串口通信3，也可以使用其他串口，需要重新配置，注意波特率（不同厂商的波特率不同）
usart3.h
hc05.c初始化蓝牙模块，内置接受和发送的缓冲器，可供读取
hc05.h
如需必要，可以将蓝牙的接受和发送模块单独写入一个文件
默认状态下：
接收时是USART3_RX_BUF，返回的是字符串
发送时是u3_printf("\r\n");，发送给手机

如果想通过串口交互的方式修改蓝牙模块的配置
可以调用USMART文件夹中的文件
具体配置情况有待研究
